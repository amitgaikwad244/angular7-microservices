import { GreaterThanDirective } from './greater-than.directive';

describe('GreaterThanDirective', () => {
  it('should create an instance', () => {
    const directive = new GreaterThanDirective();
    expect(directive).toBeTruthy();
  });
});
