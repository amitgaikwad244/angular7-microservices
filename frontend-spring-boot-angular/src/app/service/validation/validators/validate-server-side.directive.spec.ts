import { ValidateServerSideDirective } from './validate-server-side.directive';

describe('ValidateServerSideDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateServerSideDirective();
    expect(directive).toBeTruthy();
  });
});
