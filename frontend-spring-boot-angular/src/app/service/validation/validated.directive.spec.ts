import { ValidatedDirective } from "./validated.directive";


describe('ValidatedDirective', () => {
  it('should create an instance', () => {
    // const directive = new ValidatedDirective();
    // expect(directive).toBeTruthy();
  });
});
